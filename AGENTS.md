paper cli ： 专注生成本地离线文档的 cli；

价值理念： 
让 AI 使用 paper cli 快速生成专业风格的文档，如 docx、pdf、html；
基于固定的文档模板 + 动态数据 + 动态文字， 结合 AI 动态构建数据 ， 生成质量稳定的文档

实现理念：
代码硬编码 = 一个文档模板；系统会存在大量内置的优秀的代码模块文档；
例如一个好的文档A = 基础文档整体框架样式 + 多个不同的组件 ， 如 A-w1 , A-w2, A-w3 组成；
基础文档整体框架保证整体样式和文档 A 保持一致； 不同的组件可以自由组装，是为了新的文档生成场景下
随着数据变化而自动选择的灵活性； 所以这里需要子命令， 指定文档的格式后， 可以 list 组件出来；
方便 AI 理解并且自由组装；

如何新增一个新的文档模板： coding agent 手动去分解文档和硬编码增加即可，不考虑扩展性。

如何选择合适的文档： paper cli 使用 命令去 list 文档适合什么场景的描述， 每一个文档模板需要内置 description； 组件也是存在描述，方便组合；

详细设计在 docs 下的 txt 文档；

skills文件夹 ： paper 需要配套 skill 协助说明如何使用这个 cli ， 记得及时修改，保持和逻辑保持一致；
