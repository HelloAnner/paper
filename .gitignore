# 依赖与构建产物
node_modules/
dist/
out/

# 字体（体积大，用 make fonts 重新下载，不进仓库）
cli/assets/fonts/*.otf
cli/assets/fonts/*.ttf
cli/assets/fonts/*.ttc

# 示例/迁移产物：里面通常是真实内部文档（正文与示意图），默认不入公开仓库。
# 需要公开时，把内容换成脱敏样例，或只保留脚本、排除数据与图。
examples/

# 系统与编辑器
.DS_Store
*.log
